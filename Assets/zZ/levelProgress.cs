using UnityEngine;

public class levelProgress : MonoBehaviour
{
    private void Start()
    {
        if (PlayerPrefs.GetInt("mainProgress") == 0)
        {
            
        }
    }
}
